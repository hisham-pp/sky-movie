import { bind } from './client';

export const windowMinimize = bind('windowMinimize');
export const windowMaximize = bind('windowMaximize');
export const windowClose = bind('windowClose');
export const openExternalUrl = bind('openExternalUrl');
